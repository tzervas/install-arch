# Dev Container Environment Configuration Template
# Copy this file to .env and customize the values for your environment
# This file contains build arguments and environment variables for the devcontainer

# Build Arguments (passed to Dockerfile)
# MODE: How the repository should be handled
# - mount: Use mounted workspace (default for development)
# - clone: Clone repository into container
# - user: User-provided setup
DEVCONTAINER_MODE=mount

# BRANCH: Git branch to use when cloning
DEVCONTAINER_BRANCH=main

# DEPTH: Git clone depth
# - full: Full clone
# - shallow: Shallow clone (--depth 1)
DEVCONTAINER_DEPTH=full

# VERSION: Package version to install
# - latest: Install from main branch
# - specific version: e.g., "1.0.0"
DEVCONTAINER_VERSION=latest

# REPO_URL: Repository URL for cloning/installing
DEVCONTAINER_REPO_URL=https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Runtime Environment Variables
# Add any additional environment variables needed in the container
# DEVCONTAINER_DEBUG=true
# DEVCONTAINER_VERBOSE=true

# Sensitive Configuration
# Add sensitive values here that shouldn't be committed to git
# API_KEYS, TOKENS, SECRETS, etc.
# GITHUB_TOKEN=your_github_token_here
# DOCKER_REGISTRY_TOKEN=your_registry_token_here