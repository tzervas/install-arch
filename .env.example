# Install Arch Environment Configuration Template
# Copy this file to .env and customize the values for your environment
# This file contains sensitive configuration that should not be committed to git
# SENSITIVE VALUES - DO NOT COMMIT .env TO GIT

# =============================================================================
# INSTALLER SECRETS - REQUIRED FOR AUTOMATED INSTALLATION
# =============================================================================

# LUKS Encryption Passwords (for disk encryption)
# These must be set before running the installer
# Use a strong, unique password for disk encryption
INSTALL_ARCH_LUKS_PASSWORD=your_strong_luks_password_here
INSTALL_ARCH_LUKS_PASSWORD_CONFIRM=your_strong_luks_password_here

# User Account Passwords
# Initial password for the main user account
# You will be forced to change this on first login
INSTALL_ARCH_USER_PASSWORD=your_initial_user_password
INSTALL_ARCH_USER_PASSWORD_CONFIRM=your_initial_user_password

# Root Password (optional, defaults to user password if not set)
# Separate password for root/sudo access
# INSTALL_ARCH_ROOT_PASSWORD=your_root_password
# INSTALL_ARCH_ROOT_PASSWORD_CONFIRM=your_root_password

# SSH Keys and Access
# SSH public keys to add to authorized_keys (one per line)
# Generate with: ssh-keygen -t ed25519 -C "your_email@example.com"
# INSTALL_ARCH_SSH_AUTHORIZED_KEYS="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... user@host"

# SSH Private Key (for automated SSH access to installed system)
# Base64 encoded private key content
# INSTALL_ARCH_SSH_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----\n..."

# API Tokens and External Services
# GitHub Personal Access Token (for API access, repository management)
# Create at: https://github.com/settings/tokens
# INSTALL_ARCH_GITHUB_TOKEN=ghp_your_github_token_here

# Docker Registry Credentials
# For private registries or Docker Hub API access
# INSTALL_ARCH_DOCKER_USERNAME=your_docker_username
# INSTALL_ARCH_DOCKER_PASSWORD=your_docker_password
# INSTALL_ARCH_DOCKER_REGISTRY_TOKEN=your_registry_token

# Backup Server Credentials (for automated backups)
# Credentials for rsync/ssh backup destinations
# INSTALL_ARCH_BACKUP_SERVER_USER=backup_user
# INSTALL_ARCH_BACKUP_SERVER_PASSWORD=backup_password
# INSTALL_ARCH_BACKUP_ENCRYPTION_KEY=your_backup_encryption_key

# Monitoring and Logging Services
# API keys for external monitoring/logging services
# INSTALL_ARCH_LOGGING_API_KEY=your_logging_service_key
# INSTALL_ARCH_MONITORING_API_KEY=your_monitoring_service_key

# Cloud Provider Credentials (if deploying to cloud)
# INSTALL_ARCH_AWS_ACCESS_KEY_ID=your_aws_access_key
# INSTALL_ARCH_AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# INSTALL_ARCH_DO_API_TOKEN=your_digitalocean_token

# =============================================================================
# DEBIAN INSTALLER SECRETS
# =============================================================================

# Debian LUKS Encryption Passwords (for disk encryption)
# These must be set before running the Debian installer
# Use a strong, unique password for disk encryption
INSTALL_DEBIAN_LUKS_PASSWORD=your_strong_luks_password_here
INSTALL_DEBIAN_LUKS_PASSWORD_CONFIRM=your_strong_luks_password_here

# Debian User Account Passwords
# Initial password for the main user account
# You will be forced to change this on first login
INSTALL_DEBIAN_USER_PASSWORD=your_initial_user_password
INSTALL_DEBIAN_USER_PASSWORD_CONFIRM=your_initial_user_password

# Debian Root Password (optional, defaults to user password if not set)
# Separate password for root/sudo access
# INSTALL_DEBIAN_ROOT_PASSWORD=your_root_password
# INSTALL_DEBIAN_ROOT_PASSWORD_CONFIRM=your_root_password

# =============================================================================
# CONFIGURATION OVERRIDES (Optional)
# =============================================================================

# Application settings
# INSTALL_ARCH_APPLICATION_NAME=install-arch
# INSTALL_ARCH_APPLICATION_VERSION=1.1.1

# URL configurations
# INSTALL_ARCH_URLS_UV_INSTALL_URL=https://astral.sh/uv/install.sh
# INSTALL_ARCH_URLS_POETRY_INSTALL_URL=https://install.python-poetry.org
# INSTALL_ARCH_URLS_ARCH_MIRRORS="https://mirror.rackspace.com/archlinux/iso https://geo.mirror.pkgbuild.com/iso https://mirrors.kernel.org/archlinux/iso"
# INSTALL_ARCH_URLS_VENTOY_BASE_URL=https://github.com/ventoy/Ventoy/releases/download

# Version settings
# INSTALL_ARCH_VERSIONS_ARCH_ISO_VERSION=2025.12.01
# INSTALL_ARCH_VERSIONS_VENTOY_VERSION=1.0.99
# INSTALL_ARCH_VERSIONS_PYTHON_MIN_VERSION=3.11

# Path configurations
# INSTALL_ARCH_PATHS_WORKSPACE_DIR=/workspaces/install-arch
# INSTALL_ARCH_PATHS_TEMP_BASE_DIR=/tmp/install-arch
# INSTALL_ARCH_PATHS_ISO_DIR=iso
# INSTALL_ARCH_PATHS_CONFIG_DIR=configs
# INSTALL_ARCH_PATHS_USB_DEVICE=/dev/sdb

# Network settings
# INSTALL_ARCH_NETWORK_DNS_TEST_IP=8.8.8.8
# INSTALL_ARCH_NETWORK_DOCKER_REGISTRY_URL=https://index.docker.io/v2/

# Virtualization settings
# INSTALL_ARCH_VIRTUALIZATION_QEMU_MONITOR_PORT=4444
# INSTALL_ARCH_VIRTUALIZATION_QEMU_QMP_PORT=4445
# INSTALL_ARCH_VIRTUALIZATION_QEMU_HOST_IP=127.0.0.1

# Security settings
# INSTALL_ARCH_SECURITY_SECURE_TEMP_ENABLED=true
# INSTALL_ARCH_SECURITY_VALIDATE_CHECKSUMS=true
# INSTALL_ARCH_SECURITY_REQUIRE_ENCRYPTION=true

# Development settings (legacy compatibility)
# INSTALL_ARCH_DEVELOPMENT_PACKAGE_MANAGER=uv
# INSTALL_ARCH_DEVELOPMENT_VENV_PATH=.venv
# INSTALL_ARCH_DEVELOPMENT_PYTHON_VERSION=3.11
# INSTALL_ARCH_DEVELOPMENT_USE_GIT_OPS=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env with your actual values:
#    nano .env
#
# 3. Set secure permissions:
#    chmod 600 .env
#
# 4. Never commit .env to git (it's in .gitignore)
#
# 5. For production deployments, use different strong passwords
#    and rotate API tokens regularly